spring:
  datasource:
    url: jdbc:mysql://localhost:3306/socks?useSSL=false
    username: root
    password: 123456
    driver-class-name: com.mysql.jdbc.Driver

  thymeleaf:
    cache: false #关闭缓存
    prefix: classpath:/views/ #调整页面路径（原来可能是templates/）

mybatis:
  configuration:
    map-underscore-to-camel-case: true

logging:
  level:
    #打印SQL信息
    com.hehe.mabatis.mapper: debug


